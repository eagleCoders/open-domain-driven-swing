/**
 * 
 */
package com.eagle.coders.swing.core.ui.jpa;

import java.util.Map;

import javax.persistence.EntityManagerFactory;
import javax.persistence.spi.PersistenceUnitInfo;

import org.hibernate.ejb.Ejb3Configuration;
import org.hibernate.ejb.packaging.PersistenceMetadata;

/**
 * @author Anees
 *
 */
public class EagleCodersPersistence {

	/**
	 * 
	 * @param metadata
	 * @param overrideProperties
	 * @return
	 */
	public static EntityManagerFactory createEntityManagerFactory(PersistenceMetadata metadata, Map overrideProperties) {
		
		EagleCoderEJB3Configuration cfg = new EagleCoderEJB3Configuration();
		
//		EagleCoderEJB3Configuration configured = cfg.configure(metadata, overrideProperties);
		
		
//		return configured != null ? configured.buildEntityManagerFactory() : null;
		
		return null;
	}
	
//	public EntityManagerFactory createContainerEntityManagerFactory(PersistenceUnitInfo info, Map map) {
//		Ejb3Configuration cfg = new Ejb3Configuration();
//		Ejb3Configuration configured = cfg.configure( info, map );
//		return configured != null ? configured.buildEntityManagerFactory() : null;
//	}
}
